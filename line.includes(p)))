const fs=require('fs'); const s=fs.readFileSync('src/App.jsx','utf8').split(/\r?\n/); const pats=['clearBreedingSessionFromBoard','createObservedLiveSession','onCreateObservedLiveSession','onClearPlannerSession','unassignBreedingSession']; s.forEach((line,i)= if (pats.some(p= console.log((i+1)+': '+line); }); 
