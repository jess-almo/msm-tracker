Monster Metadata Request Queue

CORE APP MONSTERS

[Boskus]
Priority: High

Known:
- id: boskus
- category: amber
- breedableOn: Amber Island, Faerie Island, Fire Oasis, Light Island, Mirror Faerie Island, Mirror Light Island
- combo: Kayna + Mammott

Missing:
- elements
- description
- class
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Boskus". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Bowgart]
Priority: High

Known:
- id: bowgart
- category: natural
- breedableOn: Cold Island, Gold Island, Mirror Cold Island, Mirror Plant Island, Plant Island, The Colossingum, Tribal Island
- combo: Potbelly + Maw

Missing:
- elements
- description
- class
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Bowgart". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Bridg-It]
Priority: High

Known:
- id: bridgit
- category: natural
- breedableOn: Faerie Island, Magical Nexus, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Bridg-It". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Bulbo]
Priority: High

Known:
- id: bulbo
- category: natural
- breedableOn: Light Island, Magical Nexus, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Bulbo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Cantorell]
Priority: High

Known:
- id: cantorell
- category: natural
- breedableOn: Faerie Island, Magical Nexus, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Cantorell". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Drumpler]
Priority: High

Known:
- id: drumpler
- category: natural
- breedableOn: Air Island, Earth Island, Faerie Island, Gold Island, Mirror Air Island, Mirror Earth Island, Mirror Faerie Island, Mirror Plant Island, Plant Island, The Colossingum, Tribal Island
- combo: Noggin + Mammott

Missing:
- elements
- description
- class
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Drumpler". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Floogull]
Priority: High

Known:
- id: floogull
- category: natural
- breedableOn: Amber Island, Fire Haven

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Floogull". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Furcorn]
Priority: High

Known:
- id: furcorn
- category: natural
- breedableOn: Cold Island, Earth Island, Gold Island, Light Island, Mirror Cold Island, Mirror Earth Island, Mirror Light Island, Mirror Plant Island, Plant Island, Shugabush Island, The Colossingum, Tribal Island
- combo: Mammott + Noggin

Missing:
- elements
- description
- class
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Furcorn". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Fwog]
Priority: High

Known:
- id: fwog
- category: natural
- breedableOn: Air Island, Bone Island, Gold Island, Mirror Air Island, Mirror Bone Island, Mirror Plant Island, Mirror Water Island, Plant Island, The Colossingum, Tribal Island, Water Island
- combo: Toe Jammer + Noggin

Missing:
- elements
- description
- class
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Fwog". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Kayna]
Priority: High

Known:
- id: kayna
- category: natural
- breedableOn: Amber Island, Bone Island, Faerie Island, Fire Haven, Fire Oasis, Light Island, Mirror Bone Island, Mirror Faerie Island, Mirror Light Island, Mirror Psychic Island, Psychic Island, Tribal Island
- combo: 4-element + Kayna

Missing:
- elements
- description
- class
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Kayna". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Pango]
Priority: High

Known:
- id: pango
- category: natural
- breedableOn: Air Island, Cold Island, Earth Island, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, Mirror Earth Island, The Colossingum, Tribal Island
- combo: Tweedle + Mammott

Missing:
- elements
- description
- class
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Pango". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Phangler]
Priority: High

Known:
- id: phangler
- category: natural
- breedableOn: Amber Island, Bone Island, Fire Oasis, Mirror Bone Island, Mirror Psychic Island, Psychic Island
- combo: Toe Jammer + Potbelly

Missing:
- elements
- description
- class
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Phangler". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[PomPom]
Priority: High

Known:
- id: pompom
- category: natural
- breedableOn: Air Island, Earth Island, Gold Island, Mirror Air Island, Mirror Earth Island, Shugabush Island, The Colossingum, Tribal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "PomPom". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Pummel]
Priority: High

Known:
- id: pummel
- category: natural
- breedableOn: Gold Island, Mirror Plant Island, Mirror Water Island, Plant Island, The Colossingum, Tribal Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Pummel". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Quarrister]
Priority: High

Known:
- id: quarrister
- category: natural
- breedableOn: Earth Island, Gold Island, Mirror Earth Island, The Colossingum, Tribal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Quarrister". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Riff]
Priority: High

Known:
- id: riff
- category: natural
- breedableOn: Air Island, Gold Island, Mirror Air Island, The Colossingum, Tribal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Riff". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Shrubb]
Priority: High

Known:
- id: shrubb
- category: natural
- breedableOn: Earth Island, Fire Haven, Gold Island, Mirror Earth Island, Mirror Plant Island, Mirror Water Island, Plant Island, The Colossingum, Tribal Island, Water Island
- combo: Potbelly + Noggin

Missing:
- elements
- description
- class
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Shrubb". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Stogg]
Priority: High

Known:
- id: stogg
- category: natural
- breedableOn: Amber Island, Bone Island, Faerie Island, Fire Haven, Mirror Bone Island, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Stogg". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[T-Rox]
Priority: High

Known:
- id: trox
- category: natural
- breedableOn: Air Island, Gold Island, Mirror Air Island, Mirror Plant Island, Plant Island, The Colossingum, Tribal Island
- combo: Noggin + Drumpler

Missing:
- elements
- description
- class
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "T-Rox". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Thrumble]
Priority: High

Known:
- id: thrumble
- category: natural
- breedableOn: Amber Island, Bone Island, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Thrumble". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Wynq]
Priority: High

Known:
- id: wynq
- category: natural
- breedableOn: Amber Island, Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Wynq". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Ziggurab]
Priority: High

Known:
- id: ziggurab
- category: amber
- breedableOn: Amber Island, Faerie Island, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- This monster already appears in the tracker flow, so richer metadata will immediately improve planning and directory usefulness.
- Improves filtering, grouping, and future search behavior across monster-facing UI.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Ziggurab". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

HIGH PRIORITY

No monsters currently queued.

MEDIUM PRIORITY

[Adult Attmoz]
Priority: Medium

Known:
- id: adultattmoz
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Adult Attmoz". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Adult Blasoom]
Priority: Medium

Known:
- id: adultblasoom
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Adult Blasoom". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Adult Furnoss]
Priority: Medium

Known:
- id: adultfurnoss
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Adult Furnoss". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Adult Galvana]
Priority: Medium

Known:
- id: adultgalvana
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Adult Galvana". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Adult Glaishur]
Priority: Medium

Known:
- id: adultglaishur
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Adult Glaishur". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Adult Hornacle]
Priority: Medium

Known:
- id: adulthornacle
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Adult Hornacle". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Adult Loodvigg]
Priority: Medium

Known:
- id: adultloodvigg
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Adult Loodvigg". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Adult Plixie]
Priority: Medium

Known:
- id: adultplixie
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Adult Plixie". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Adult Scaratar]
Priority: Medium

Known:
- id: adultscaratar
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Adult Scaratar". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Adult Syncopite]
Priority: Medium

Known:
- id: adultsyncopite
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Adult Syncopite". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Adult Tortt]
Priority: Medium

Known:
- id: adulttortt
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Adult Tortt". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Adult Vhamp]
Priority: Medium

Known:
- id: adultvhamp
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Adult Vhamp". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Anglow]
Priority: Medium

Known:
- id: anglow
- category: natural
- breedableOn: Mirror Water Island, Mythical Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Anglow". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Arackulele]
Priority: Medium

Known:
- id: arackulele
- category: natural
- breedableOn: Ethereal Island, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Arackulele". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Astropod]
Priority: Medium

Known:
- id: astropod
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Astropod". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Attmoz]
Priority: Medium

Known:
- id: attmoz
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Attmoz". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Auglur]
Priority: Medium

Known:
- id: auglur
- category: natural
- breedableOn: Ethereal Workshop, Plasma Islet

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Auglur". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Banjaw]
Priority: Medium

Known:
- id: banjaw
- category: natural
- breedableOn: Bone Island, Magical Nexus, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Banjaw". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Barrb]
Priority: Medium

Known:
- id: barrb
- category: natural
- breedableOn: Amber Island, Fire Haven

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Barrb". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Bellowfish]
Priority: Medium

Known:
- id: bellowfish
- category: natural
- breedableOn: Ethereal Island, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Bellowfish". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[BeMeebEth]
Priority: Medium

Known:
- id: bemeebeth
- category: natural
- breedableOn: Ethereal Workshop

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "BeMeebEth". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Bisonorus]
Priority: Medium

Known:
- id: bisonorus
- category: natural
- breedableOn: Amber Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Bisonorus". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Blabbit]
Priority: Medium

Known:
- id: blabbit
- category: natural
- breedableOn: Gold Island, Mirror Water Island, Seasonal Shanty, The Colossingum, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Blabbit". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Blarret]
Priority: Medium

Known:
- id: blarret
- category: natural
- breedableOn: Ethereal Workshop, Plasma Islet

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Blarret". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Blasoom]
Priority: Medium

Known:
- id: blasoom
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Blasoom". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Bleatnik]
Priority: Medium

Known:
- id: bleatnik
- category: natural
- breedableOn: Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Bleatnik". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Blipsqeak]
Priority: Medium

Known:
- id: blipsqeak
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Blipsqeak". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Blow't]
Priority: Medium

Known:
- id: blowt
- category: natural
- breedableOn: Light Island, Magical Nexus, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Blow't". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Bona-Petite]
Priority: Medium

Known:
- id: bonapetite
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Bona-Petite". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Bonkers]
Priority: Medium

Known:
- id: bonkers
- category: natural
- breedableOn: Magical Nexus, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Bonkers". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Boo'qwurm]
Priority: Medium

Known:
- id: booqwurm
- category: natural
- breedableOn: Mirror Psychic Island, Psychic Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Boo'qwurm". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Boodoo]
Priority: Medium

Known:
- id: boodoo
- category: natural
- breedableOn: Ethereal Island, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Boodoo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Bowhead]
Priority: Medium

Known:
- id: bowhead
- category: natural
- breedableOn: Amber Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Bowhead". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Brump]
Priority: Medium

Known:
- id: brump
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Brump". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Buzzinga]
Priority: Medium

Known:
- id: buzzinga
- category: natural
- breedableOn: Fire Haven, Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Buzzinga". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Cahoot]
Priority: Medium

Known:
- id: cahoot
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Cahoot". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Candelavra]
Priority: Medium

Known:
- id: candelavra
- category: natural
- breedableOn: Amber Island, Fire Haven

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Candelavra". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Carillong]
Priority: Medium

Known:
- id: carillong
- category: natural
- breedableOn: Magical Sanctum, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Carillong". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Cataliszt]
Priority: Medium

Known:
- id: cataliszt
- category: natural
- breedableOn: Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Cataliszt". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Cherubble]
Priority: Medium

Known:
- id: cherubble
- category: natural
- breedableOn: Fire Oasis, Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Cherubble". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Clackula]
Priority: Medium

Known:
- id: clackula
- category: natural
- breedableOn: Bone Island, Magical Nexus, Magical Sanctum, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Clackula". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Clamble]
Priority: Medium

Known:
- id: clamble
- category: natural
- breedableOn: Earth Island, Gold Island, Mirror Earth Island, Mirror Plant Island, Plant Island, The Colossingum, Tribal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Clamble". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Clavavera]
Priority: Medium

Known:
- id: clavavera
- category: natural
- breedableOn: Bone Island, Mirror Bone Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Clavavera". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Clavi Gnat]
Priority: Medium

Known:
- id: clavignat
- category: natural
- breedableOn: Faerie Island, Magical Nexus, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Clavi Gnat". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Congole]
Priority: Medium

Known:
- id: congole
- category: natural
- breedableOn: Air Island, Cold Island, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, The Colossingum, Tribal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Congole". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Cranchee]
Priority: Medium

Known:
- id: cranchee
- category: natural
- breedableOn: Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Cranchee". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Creepuscule]
Priority: Medium

Known:
- id: creepuscule
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Creepuscule". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Cruv'laaphtian Crocus]
Priority: Medium

Known:
- id: cruvlaaphtiancrocus
- category: natural
- breedableOn: Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Cruv'laaphtian Crocus". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Cybop]
Priority: Medium

Known:
- id: cybop
- category: natural
- breedableOn: Air Island, Earth Island, Fire Haven, Gold Island, Mirror Air Island, Mirror Earth Island, Mirror Water Island, The Colossingum, Tribal Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Cybop". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Dandidoo]
Priority: Medium

Known:
- id: dandidoo
- category: natural
- breedableOn: Cold Island, Earth Island, Fire Haven, Gold Island, Mirror Cold Island, Mirror Earth Island, Mirror Water Island, The Colossingum, Tribal Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Dandidoo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Deedge]
Priority: Medium

Known:
- id: deedge
- category: natural
- breedableOn: Cold Island, Gold Island, Mirror Cold Island, Shugabush Island, The Colossingum, Tribal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Deedge". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Déjà-Jin]
Priority: Medium

Known:
- id: djjin
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Déjà-Jin". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Denchuhs]
Priority: Medium

Known:
- id: denchuhs
- category: natural
- breedableOn: Bone Island, Magical Nexus, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Denchuhs". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Dermit]
Priority: Medium

Known:
- id: dermit
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Dermit". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Do]
Priority: Medium

Known:
- id: do
- category: natural
- breedableOn: Air Island, Bone Island, Cold Island, Earth Island, Ethereal Island, Faerie Island, Fire Haven, Fire Oasis, Light Island, Magical Sanctum, Mirror Air Island, Mirror Bone Island, Mirror Cold Island, Mirror Earth Island, Mirror Faerie Island, Mirror Light Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Do". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Dragong]
Priority: Medium

Known:
- id: dragong
- category: natural
- breedableOn: Ethereal Island, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Dragong". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Drummidary]
Priority: Medium

Known:
- id: drummidary
- category: natural
- breedableOn: Amber Island, Light Island, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Drummidary". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Dwumrohl]
Priority: Medium

Known:
- id: dwumrohl
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Dwumrohl". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Edamimi]
Priority: Medium

Known:
- id: edamimi
- category: natural
- breedableOn: Amber Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Edamimi". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Enchantling]
Priority: Medium

Known:
- id: enchantling
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Enchantling". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Entbrat]
Priority: Medium

Known:
- id: entbrat
- category: natural
- breedableOn: Gold Island, Mirror Plant Island, Plant Island, The Colossingum, Tribal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Entbrat". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Anglow]
Priority: Medium

Known:
- id: epicanglow
- category: natural
- breedableOn: Mirror Water Island, Mythical Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Anglow". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Arackulele]
Priority: Medium

Known:
- id: epicarackulele
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Arackulele". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Barrb]
Priority: Medium

Known:
- id: epicbarrb
- category: natural
- breedableOn: Amber Island, Fire Haven

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Barrb". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Bellowfish]
Priority: Medium

Known:
- id: epicbellowfish
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Bellowfish". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Blabbit]
Priority: Medium

Known:
- id: epicblabbit
- category: natural
- breedableOn: Gold Island, Mirror Water Island, Seasonal Shanty, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Blabbit". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Boo'qwurm]
Priority: Medium

Known:
- id: epicbooqwurm
- category: natural
- breedableOn: Mirror Psychic Island, Psychic Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Boo'qwurm". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Boodoo]
Priority: Medium

Known:
- id: epicboodoo
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Boodoo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Boskus]
Priority: Medium

Known:
- id: epicboskus
- category: amber
- breedableOn: Amber Island, Faerie Island, Fire Oasis, Light Island, Mirror Faerie Island, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Boskus". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Bowgart]
Priority: Medium

Known:
- id: epicbowgart
- category: natural
- breedableOn: Cold Island, Gold Island, Mirror Cold Island, Mirror Plant Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Bowgart". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Brump]
Priority: Medium

Known:
- id: epicbrump
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Brump". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Buzzinga]
Priority: Medium

Known:
- id: epicbuzzinga
- category: natural
- breedableOn: Fire Haven, Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Buzzinga". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Carillong]
Priority: Medium

Known:
- id: epiccarillong
- category: natural
- breedableOn: Magical Sanctum, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Carillong". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Cherubble]
Priority: Medium

Known:
- id: epiccherubble
- category: natural
- breedableOn: Fire Oasis, Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Cherubble". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Clamble]
Priority: Medium

Known:
- id: epicclamble
- category: natural
- breedableOn: Earth Island, Gold Island, Mirror Earth Island, Mirror Plant Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Clamble". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Clavavera]
Priority: Medium

Known:
- id: epicclavavera
- category: natural
- breedableOn: Bone Island, Mirror Bone Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Clavavera". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Congole]
Priority: Medium

Known:
- id: epiccongole
- category: natural
- breedableOn: Air Island, Cold Island, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Congole". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Cybop]
Priority: Medium

Known:
- id: epiccybop
- category: natural
- breedableOn: Air Island, Earth Island, Fire Haven, Gold Island, Mirror Air Island, Mirror Earth Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Cybop". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Dandidoo]
Priority: Medium

Known:
- id: epicdandidoo
- category: natural
- breedableOn: Cold Island, Earth Island, Fire Haven, Gold Island, Mirror Cold Island, Mirror Earth Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Dandidoo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Deedge]
Priority: Medium

Known:
- id: epicdeedge
- category: natural
- breedableOn: Cold Island, Gold Island, Mirror Cold Island, Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Deedge". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Dragong]
Priority: Medium

Known:
- id: epicdragong
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Dragong". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Drumpler]
Priority: Medium

Known:
- id: epicdrumpler
- category: natural
- breedableOn: Air Island, Earth Island, Faerie Island, Gold Island, Mirror Air Island, Mirror Earth Island, Mirror Faerie Island, Mirror Plant Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Drumpler". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Dwumrohl]
Priority: Medium

Known:
- id: epicdwumrohl
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Dwumrohl". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Edamimi]
Priority: Medium

Known:
- id: epicedamimi
- category: natural
- breedableOn: Amber Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Edamimi". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Entbrat]
Priority: Medium

Known:
- id: epicentbrat
- category: natural
- breedableOn: Gold Island, Mirror Plant Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Entbrat". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Ffidyll]
Priority: Medium

Known:
- id: epicffidyll
- category: natural
- breedableOn: Faerie Island, Mirror Faerie Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Ffidyll". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Floogull]
Priority: Medium

Known:
- id: epicfloogull
- category: natural
- breedableOn: Amber Island, Fire Haven

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Floogull". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Flowah]
Priority: Medium

Known:
- id: epicflowah
- category: natural
- breedableOn: Amber Island, Fire Haven, Light Island, Mirror Light Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Flowah". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Fung Pray]
Priority: Medium

Known:
- id: epicfungpray
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Fung Pray". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Furcorn]
Priority: Medium

Known:
- id: epicfurcorn
- category: natural
- breedableOn: Cold Island, Earth Island, Gold Island, Light Island, Mirror Cold Island, Mirror Earth Island, Mirror Light Island, Mirror Plant Island, Plant Island, Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Furcorn". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Fwog]
Priority: Medium

Known:
- id: epicfwog
- category: natural
- breedableOn: Air Island, Bone Island, Gold Island, Mirror Air Island, Mirror Bone Island, Mirror Plant Island, Mirror Water Island, Plant Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Fwog". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic G'Joob]
Priority: Medium

Known:
- id: epicgjoob
- category: natural
- breedableOn: Mirror Plant Island, Mythical Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic G'Joob". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Ghazt]
Priority: Medium

Known:
- id: epicghazt
- category: natural
- breedableOn: Ethereal Island, Mirror Plant Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Ghazt". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Glowl]
Priority: Medium

Known:
- id: epicglowl
- category: natural
- breedableOn: Amber Island, Fire Haven, Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Glowl". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Gobbleygourd]
Priority: Medium

Known:
- id: epicgobbleygourd
- category: natural
- breedableOn: Fire Haven, Fire Oasis, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Gobbleygourd". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Grumpyre]
Priority: Medium

Known:
- id: epicgrumpyre
- category: natural
- breedableOn: Cold Island, Ethereal Island, Mirror Cold Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Grumpyre". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Hoola]
Priority: Medium

Known:
- id: epichoola
- category: natural
- breedableOn: Air Island, Earth Island, Gold Island, Mirror Air Island, Mirror Earth Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Hoola". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Humbug]
Priority: Medium

Known:
- id: epichumbug
- category: natural
- breedableOn: Earth Island, Ethereal Island, Mirror Earth Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Humbug". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Jam Boree]
Priority: Medium

Known:
- id: epicjamboree
- category: natural
- breedableOn: Gold Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Jam Boree". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Jeeode]
Priority: Medium

Known:
- id: epicjeeode
- category: natural
- breedableOn: Ethereal Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Jeeode". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Jellbilly]
Priority: Medium

Known:
- id: epicjellbilly
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Jellbilly". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Kayna]
Priority: Medium

Known:
- id: epickayna
- category: natural
- breedableOn: Amber Island, Bone Island, Faerie Island, Fire Haven, Fire Oasis, Light Island, Mirror Bone Island, Mirror Faerie Island, Mirror Light Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Kayna". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Kazilleon]
Priority: Medium

Known:
- id: epickazilleon
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Kazilleon". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Krillby]
Priority: Medium

Known:
- id: epickrillby
- category: natural
- breedableOn: Amber Island, Faerie Island, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Krillby". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Mammott]
Priority: Medium

Known:
- id: epicmammott
- category: natural
- breedableOn: Air Island, Cold Island, Earth Island, Faerie Island, Fire Oasis, Gold Island, Light Island, Mirror Air Island, Mirror Cold Island, Mirror Earth Island, Mirror Faerie Island, Mirror Light Island, Mirror Plant Island, Plant Island, Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Mammott". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Maw]
Priority: Medium

Known:
- id: epicmaw
- category: natural
- breedableOn: Air Island, Cold Island, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, Mirror Plant Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Maw". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Monculus]
Priority: Medium

Known:
- id: epicmonculus
- category: natural
- breedableOn: Ethereal Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Monculus". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Nebulob]
Priority: Medium

Known:
- id: epicnebulob
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Nebulob". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Noggin]
Priority: Medium

Known:
- id: epicnoggin
- category: natural
- breedableOn: Air Island, Bone Island, Earth Island, Faerie Island, Fire Haven, Gold Island, Mirror Air Island, Mirror Bone Island, Mirror Earth Island, Mirror Faerie Island, Mirror Plant Island, Mirror Water Island, Plant Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Noggin". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Oaktopus]
Priority: Medium

Known:
- id: epicoaktopus
- category: natural
- breedableOn: Cold Island, Gold Island, Mirror Cold Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, Shugabush Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Oaktopus". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Pango]
Priority: Medium

Known:
- id: epicpango
- category: natural
- breedableOn: Air Island, Cold Island, Earth Island, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, Mirror Earth Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Pango". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Phangler]
Priority: Medium

Known:
- id: epicphangler
- category: natural
- breedableOn: Amber Island, Bone Island, Fire Oasis, Mirror Bone Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Phangler". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Poewk]
Priority: Medium

Known:
- id: epicpoewk
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Poewk". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic PomPom]
Priority: Medium

Known:
- id: epicpompom
- category: natural
- breedableOn: Air Island, Earth Island, Gold Island, Mirror Air Island, Mirror Earth Island, Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic PomPom". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic PongPing]
Priority: Medium

Known:
- id: epicpongping
- category: natural
- breedableOn: Amber Island, Faerie Island, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic PongPing". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Potbelly]
Priority: Medium

Known:
- id: epicpotbelly
- category: natural
- breedableOn: Cold Island, Earth Island, Fire Haven, Gold Island, Light Island, Mirror Cold Island, Mirror Earth Island, Mirror Light Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, Shugabush Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Potbelly". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Pummel]
Priority: Medium

Known:
- id: epicpummel
- category: natural
- breedableOn: Gold Island, Mirror Plant Island, Mirror Water Island, Plant Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Pummel". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Punkleton]
Priority: Medium

Known:
- id: epicpunkleton
- category: natural
- breedableOn: Gold Island, Mirror Plant Island, Plant Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Punkleton". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Quarrister]
Priority: Medium

Known:
- id: epicquarrister
- category: natural
- breedableOn: Earth Island, Gold Island, Mirror Earth Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Quarrister". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Quibble]
Priority: Medium

Known:
- id: epicquibble
- category: natural
- breedableOn: Air Island, Cold Island, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, Mirror Water Island, Shugabush Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Quibble". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Reebro]
Priority: Medium

Known:
- id: epicreebro
- category: natural
- breedableOn: Air Island, Ethereal Island, Mirror Air Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Reebro". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Reedling]
Priority: Medium

Known:
- id: epicreedling
- category: natural
- breedableOn: Earth Island, Fire Haven, Gold Island, Mirror Earth Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Reedling". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Repatillo]
Priority: Medium

Known:
- id: epicrepatillo
- category: natural
- breedableOn: Amber Island, Fire Haven

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Repatillo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Riff]
Priority: Medium

Known:
- id: epicriff
- category: natural
- breedableOn: Air Island, Gold Island, Mirror Air Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Riff". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Rootitoot]
Priority: Medium

Known:
- id: epicrootitoot
- category: natural
- breedableOn: Amber Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Rootitoot". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Schmoochle]
Priority: Medium

Known:
- id: epicschmoochle
- category: natural
- breedableOn: Air Island, Gold Island, Mirror Air Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Schmoochle". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Scups]
Priority: Medium

Known:
- id: epicscups
- category: natural
- breedableOn: Air Island, Gold Island, Mirror Air Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Scups". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Shellbeat]
Priority: Medium

Known:
- id: epicshellbeat
- category: natural
- breedableOn: Gold Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Shellbeat". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Shrubb]
Priority: Medium

Known:
- id: epicshrubb
- category: natural
- breedableOn: Earth Island, Fire Haven, Gold Island, Mirror Earth Island, Mirror Plant Island, Mirror Water Island, Plant Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Shrubb". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Sneyser]
Priority: Medium

Known:
- id: epicsneyser
- category: natural
- breedableOn: Amber Island, Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Sneyser". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Sooza]
Priority: Medium

Known:
- id: epicsooza
- category: natural
- breedableOn: Amber Island, Light Island, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Sooza". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Sox]
Priority: Medium

Known:
- id: epicsox
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Sox". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Spunge]
Priority: Medium

Known:
- id: epicspunge
- category: natural
- breedableOn: Cold Island, Gold Island, Mirror Cold Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Spunge". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Spurrit]
Priority: Medium

Known:
- id: epicspurrit
- category: natural
- breedableOn: Fire Oasis, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Spurrit". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Stogg]
Priority: Medium

Known:
- id: epicstogg
- category: natural
- breedableOn: Amber Island, Bone Island, Faerie Island, Fire Haven, Mirror Bone Island, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Stogg". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Strombonin]
Priority: Medium

Known:
- id: epicstrombonin
- category: natural
- breedableOn: Cold Island, Mirror Cold Island, Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Strombonin". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic T-Rox]
Priority: Medium

Known:
- id: epictrox
- category: natural
- breedableOn: Air Island, Gold Island, Mirror Air Island, Mirror Plant Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic T-Rox". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Thrumble]
Priority: Medium

Known:
- id: epicthrumble
- category: natural
- breedableOn: Amber Island, Bone Island, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Thrumble". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Thumpies]
Priority: Medium

Known:
- id: epicthumpies
- category: natural
- breedableOn: Cold Island, Earth Island, Gold Island, Mirror Cold Island, Mirror Earth Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Thumpies". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Thwok]
Priority: Medium

Known:
- id: epicthwok
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Thwok". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Toe Jammer]
Priority: Medium

Known:
- id: epictoejammer
- category: natural
- breedableOn: Air Island, Bone Island, Cold Island, Fire Oasis, Gold Island, Mirror Air Island, Mirror Bone Island, Mirror Cold Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Toe Jammer". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Tring]
Priority: Medium

Known:
- id: epictring
- category: natural
- breedableOn: Amber Island, Fire Haven

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Tring". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Tweedle]
Priority: Medium

Known:
- id: epictweedle
- category: natural
- breedableOn: Air Island, Cold Island, Earth Island, Fire Haven, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, Mirror Earth Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Tweedle". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Viveine]
Priority: Medium

Known:
- id: epicviveine
- category: natural
- breedableOn: Amber Island, Seasonal Shanty, Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Viveine". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Whaddle]
Priority: Medium

Known:
- id: epicwhaddle
- category: natural
- breedableOn: Amber Island, Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Whaddle". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Whisp]
Priority: Medium

Known:
- id: epicwhisp
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Whisp". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Whiz-bang]
Priority: Medium

Known:
- id: epicwhizbang
- category: natural
- breedableOn: Light Island, Mirror Light Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Whiz-bang". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Woolabee]
Priority: Medium

Known:
- id: epicwoolabee
- category: natural
- breedableOn: Amber Island, Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Woolabee". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Wubbox]
Priority: Medium

Known:
- id: epicwubbox
- category: natural
- breedableOn: Air Island, Cold Island, Earth Island, Ethereal Island, Fire Haven, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, Mirror Earth Island, Mirror Plant Island, Mirror Water Island, Plant Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Wubbox". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Wynq]
Priority: Medium

Known:
- id: epicwynq
- category: natural
- breedableOn: Amber Island, Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Wynq". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Yawstrich]
Priority: Medium

Known:
- id: epicyawstrich
- category: natural
- breedableOn: Air Island, Mirror Air Island, Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Yawstrich". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Yelmut]
Priority: Medium

Known:
- id: epicyelmut
- category: natural
- breedableOn: Amber Island, Light Island, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Yelmut". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Yool]
Priority: Medium

Known:
- id: epicyool
- category: natural
- breedableOn: Cold Island, Gold Island, Mirror Cold Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Yool". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Ziggurab]
Priority: Medium

Known:
- id: epicziggurab
- category: amber
- breedableOn: Amber Island, Faerie Island, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Ziggurab". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Epic Zynth]
Priority: Medium

Known:
- id: epiczynth
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Epic Zynth". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Fa]
Priority: Medium

Known:
- id: fa
- category: natural
- breedableOn: Air Island, Bone Island, Cold Island, Earth Island, Ethereal Island, Faerie Island, Fire Haven, Fire Oasis, Light Island, Magical Sanctum, Mirror Air Island, Mirror Bone Island, Mirror Cold Island, Mirror Earth Island, Mirror Faerie Island, Mirror Light Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Fa". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Ffidyll]
Priority: Medium

Known:
- id: ffidyll
- category: natural
- breedableOn: Faerie Island, Mirror Faerie Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Ffidyll". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Fiddlement]
Priority: Medium

Known:
- id: fiddlement
- category: natural
- breedableOn: Light Island, Magical Nexus, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Fiddlement". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Flasque]
Priority: Medium

Known:
- id: flasque
- category: natural
- breedableOn: Ethereal Workshop, Plasma Islet

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Flasque". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Fleechwurm]
Priority: Medium

Known:
- id: fleechwurm
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Fleechwurm". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Floot Fly]
Priority: Medium

Known:
- id: flootfly
- category: natural
- breedableOn: Faerie Island, Magical Nexus, Magical Sanctum, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Floot Fly". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Flowah]
Priority: Medium

Known:
- id: flowah
- category: natural
- breedableOn: Amber Island, Fire Haven, Light Island, Mirror Light Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Flowah". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Flum Ox]
Priority: Medium

Known:
- id: flumox
- category: natural
- breedableOn: Amber Island, Bone Island, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Flum Ox". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Fluoress]
Priority: Medium

Known:
- id: fluoress
- category: natural
- breedableOn: Light Island, Magical Nexus, Magical Sanctum, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Fluoress". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Frondley]
Priority: Medium

Known:
- id: frondley
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Frondley". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Fung Pray]
Priority: Medium

Known:
- id: fungpray
- category: natural
- breedableOn: Ethereal Island, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Fung Pray". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Furnoss]
Priority: Medium

Known:
- id: furnoss
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Furnoss". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[G'Day]
Priority: Medium

Known:
- id: gday
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "G'Day". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[G'Joob]
Priority: Medium

Known:
- id: gjoob
- category: natural
- breedableOn: Mirror Plant Island, Mythical Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "G'Joob". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Gaddzooks]
Priority: Medium

Known:
- id: gaddzooks
- category: natural
- breedableOn: Ethereal Workshop, Plasma Islet

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Gaddzooks". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Galvana]
Priority: Medium

Known:
- id: galvana
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Galvana". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Ghazt]
Priority: Medium

Known:
- id: ghazt
- category: natural
- breedableOn: Ethereal Island, Mirror Plant Island, Plant Island, Plasma Islet, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Ghazt". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Gheegur]
Priority: Medium

Known:
- id: gheegur
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Gheegur". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Glaishur]
Priority: Medium

Known:
- id: glaishur
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Glaishur". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Gloptic]
Priority: Medium

Known:
- id: gloptic
- category: natural
- breedableOn: Magical Nexus, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Gloptic". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Glowl]
Priority: Medium

Known:
- id: glowl
- category: natural
- breedableOn: Amber Island, Fire Haven, Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Glowl". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Gnarls]
Priority: Medium

Known:
- id: gnarls
- category: natural
- breedableOn: Amber Island, Bone Island, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Gnarls". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Gob]
Priority: Medium

Known:
- id: gob
- category: natural
- breedableOn: Light Island, Magical Nexus, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Gob". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Gobbleygourd]
Priority: Medium

Known:
- id: gobbleygourd
- category: natural
- breedableOn: Fire Haven, Fire Oasis, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Gobbleygourd". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Grumpyre]
Priority: Medium

Known:
- id: grumpyre
- category: natural
- breedableOn: Cold Island, Ethereal Island, Mirror Cold Island, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Grumpyre". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Hairionette]
Priority: Medium

Known:
- id: hairionette
- category: natural
- breedableOn: Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Hairionette". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Hawlo]
Priority: Medium

Known:
- id: hawlo
- category: natural
- breedableOn: Bone Island, Magical Nexus, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Hawlo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[HippityHop]
Priority: Medium

Known:
- id: hippityhop
- category: natural
- breedableOn: Faerie Island, Magical Nexus, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "HippityHop". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Hoola]
Priority: Medium

Known:
- id: hoola
- category: natural
- breedableOn: Air Island, Earth Island, Gold Island, Mirror Air Island, Mirror Earth Island, Seasonal Shanty, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Hoola". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Hornacle]
Priority: Medium

Known:
- id: hornacle
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Hornacle". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Humbug]
Priority: Medium

Known:
- id: humbug
- category: natural
- breedableOn: Earth Island, Ethereal Island, Mirror Earth Island, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Humbug". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Hyehehe]
Priority: Medium

Known:
- id: hyehehe
- category: natural
- breedableOn: Earth Island, Mirror Earth Island, Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Hyehehe". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Incisaur]
Priority: Medium

Known:
- id: incisaur
- category: natural
- breedableOn: Amber Island, Bone Island, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Incisaur". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Jam Boree]
Priority: Medium

Known:
- id: jamboree
- category: natural
- breedableOn: Gold Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Jam Boree". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Jeeode]
Priority: Medium

Known:
- id: jeeode
- category: natural
- breedableOn: Ethereal Island, Mirror Water Island, The Colossingum, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Jeeode". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Jellbilly]
Priority: Medium

Known:
- id: jellbilly
- category: natural
- breedableOn: Ethereal Island, Plasma Islet, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Jellbilly". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Kazilleon]
Priority: Medium

Known:
- id: kazilleon
- category: natural
- breedableOn: Ethereal Island, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Kazilleon". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Knucklehead]
Priority: Medium

Known:
- id: knucklehead
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Knucklehead". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Knurv]
Priority: Medium

Known:
- id: knurv
- category: natural
- breedableOn: Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Knurv". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Krillby]
Priority: Medium

Known:
- id: krillby
- category: natural
- breedableOn: Amber Island, Faerie Island, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Krillby". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[La]
Priority: Medium

Known:
- id: la
- category: natural
- breedableOn: Air Island, Bone Island, Cold Island, Earth Island, Ethereal Island, Faerie Island, Fire Haven, Fire Oasis, Light Island, Magical Sanctum, Mirror Air Island, Mirror Bone Island, Mirror Cold Island, Mirror Earth Island, Mirror Faerie Island, Mirror Light Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "La". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Larvaluss]
Priority: Medium

Known:
- id: larvaluss
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Larvaluss". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Loodvigg]
Priority: Medium

Known:
- id: loodvigg
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Loodvigg". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Lowb]
Priority: Medium

Known:
- id: lowb
- category: natural
- breedableOn: Plasma Islet

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Lowb". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Maggpi]
Priority: Medium

Known:
- id: maggpi
- category: natural
- breedableOn: Cold Island, Mirror Cold Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Maggpi". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Mammott]
Priority: Medium

Known:
- id: mammott
- category: natural
- breedableOn: Air Island, Cold Island, Earth Island, Faerie Island, Fire Oasis, Gold Island, Light Island, Mirror Air Island, Mirror Cold Island, Mirror Earth Island, Mirror Faerie Island, Mirror Light Island, Mirror Plant Island, Plant Island, Shugabush Island, The Colossingum, Tribal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Mammott". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Maulch]
Priority: Medium

Known:
- id: maulch
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Maulch". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Maw]
Priority: Medium

Known:
- id: maw
- category: natural
- breedableOn: Air Island, Cold Island, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, Mirror Plant Island, Plant Island, The Colossingum, Tribal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Maw". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Meebkin]
Priority: Medium

Known:
- id: meebkin
- category: natural
- breedableOn: Ethereal Workshop, Plasma Islet

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Meebkin". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Mi]
Priority: Medium

Known:
- id: mi
- category: natural
- breedableOn: Air Island, Bone Island, Cold Island, Earth Island, Ethereal Island, Faerie Island, Fire Haven, Fire Oasis, Light Island, Magical Sanctum, Mirror Air Island, Mirror Bone Island, Mirror Cold Island, Mirror Earth Island, Mirror Faerie Island, Mirror Light Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Mi". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Mimic]
Priority: Medium

Known:
- id: mimic
- category: natural
- breedableOn: Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Mimic". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Monculus]
Priority: Medium

Known:
- id: monculus
- category: natural
- breedableOn: Ethereal Island, Seasonal Shanty, Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Monculus". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Mushaboom]
Priority: Medium

Known:
- id: mushaboom
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Mushaboom". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Nebulob]
Priority: Medium

Known:
- id: nebulob
- category: natural
- breedableOn: Ethereal Island, Plasma Islet, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Nebulob". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Nitebear]
Priority: Medium

Known:
- id: nitebear
- category: natural
- breedableOn: Ethereal Workshop

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Nitebear". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Noggin]
Priority: Medium

Known:
- id: noggin
- category: natural
- breedableOn: Air Island, Bone Island, Earth Island, Faerie Island, Fire Haven, Gold Island, Mirror Air Island, Mirror Bone Island, Mirror Earth Island, Mirror Faerie Island, Mirror Plant Island, Mirror Water Island, Plant Island, The Colossingum, Tribal Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Noggin". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Oaktopus]
Priority: Medium

Known:
- id: oaktopus
- category: natural
- breedableOn: Cold Island, Gold Island, Mirror Cold Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, Shugabush Island, The Colossingum, Tribal Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Oaktopus". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Oogiddy]
Priority: Medium

Known:
- id: oogiddy
- category: natural
- breedableOn: Ethereal Workshop

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Oogiddy". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Osstax]
Priority: Medium

Known:
- id: osstax
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Osstax". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Owlesque]
Priority: Medium

Known:
- id: owlesque
- category: natural
- breedableOn: Faerie Island, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Owlesque". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Parlsona]
Priority: Medium

Known:
- id: parlsona
- category: natural
- breedableOn: Air Island, Mirror Air Island, Mirror Plant Island, Mirror Water Island, Plant Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Parlsona". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Peckidna]
Priority: Medium

Known:
- id: peckidna
- category: natural
- breedableOn: Bone Island, Magical Nexus, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Peckidna". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Pentumbra]
Priority: Medium

Known:
- id: pentumbra
- category: natural
- breedableOn: Ethereal Workshop

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Pentumbra". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Periscorp]
Priority: Medium

Known:
- id: periscorp
- category: natural
- breedableOn: Magical Nexus, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Periscorp". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Phosphoran Phlox]
Priority: Medium

Known:
- id: phosphoranphlox
- category: natural
- breedableOn: Light Island, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Phosphoran Phlox". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Pinghound]
Priority: Medium

Known:
- id: pinghound
- category: natural
- breedableOn: Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Pinghound". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Piplash]
Priority: Medium

Known:
- id: piplash
- category: natural
- breedableOn: Ethereal Workshop

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Piplash". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Pixolotl]
Priority: Medium

Known:
- id: pixolotl
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Pixolotl". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Pladdie]
Priority: Medium

Known:
- id: pladdie
- category: natural
- breedableOn: Faerie Island, Magical Nexus, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Pladdie". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Plinkajou]
Priority: Medium

Known:
- id: plinkajou
- category: natural
- breedableOn: Bone Island, Magical Nexus, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Plinkajou". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Plixie]
Priority: Medium

Known:
- id: plixie
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Plixie". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Pluckbill]
Priority: Medium

Known:
- id: pluckbill
- category: natural
- breedableOn: Light Island, Magical Nexus, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Pluckbill". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Poewk]
Priority: Medium

Known:
- id: poewk
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Poewk". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[PongPing]
Priority: Medium

Known:
- id: pongping
- category: natural
- breedableOn: Amber Island, Faerie Island, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "PongPing". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Poppette]
Priority: Medium

Known:
- id: poppette
- category: natural
- breedableOn: Magical Nexus, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Poppette". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Potbelly]
Priority: Medium

Known:
- id: potbelly
- category: natural
- breedableOn: Cold Island, Earth Island, Fire Haven, Gold Island, Light Island, Mirror Cold Island, Mirror Earth Island, Mirror Light Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, Shugabush Island, The Colossingum, Tribal Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Potbelly". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Punkleton]
Priority: Medium

Known:
- id: punkleton
- category: natural
- breedableOn: Gold Island, Mirror Plant Island, Plant Island, Seasonal Shanty, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Punkleton". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Quibble]
Priority: Medium

Known:
- id: quibble
- category: natural
- breedableOn: Air Island, Cold Island, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, Mirror Water Island, Shugabush Island, The Colossingum, Tribal Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Quibble". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Anglow]
Priority: Medium

Known:
- id: rareanglow
- category: natural
- breedableOn: Mirror Water Island, Mythical Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Anglow". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Arackulele]
Priority: Medium

Known:
- id: rarearackulele
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Arackulele". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Astropod]
Priority: Medium

Known:
- id: rareastropod
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Astropod". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Banjaw]
Priority: Medium

Known:
- id: rarebanjaw
- category: natural
- breedableOn: Bone Island, Magical Nexus, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Banjaw". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Barrb]
Priority: Medium

Known:
- id: rarebarrb
- category: natural
- breedableOn: Amber Island, Fire Haven

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Barrb". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Bellowfish]
Priority: Medium

Known:
- id: rarebellowfish
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Bellowfish". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Bisonorus]
Priority: Medium

Known:
- id: rarebisonorus
- category: natural
- breedableOn: Amber Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Bisonorus". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Blabbit]
Priority: Medium

Known:
- id: rareblabbit
- category: natural
- breedableOn: Gold Island, Mirror Water Island, Seasonal Shanty, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Blabbit". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Bleatnik]
Priority: Medium

Known:
- id: rarebleatnik
- category: natural
- breedableOn: Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Bleatnik". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Blipsqeak]
Priority: Medium

Known:
- id: rareblipsqeak
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Blipsqeak". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Bona-Petite]
Priority: Medium

Known:
- id: rarebonapetite
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Bona-Petite". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Bonkers]
Priority: Medium

Known:
- id: rarebonkers
- category: natural
- breedableOn: Magical Nexus, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Bonkers". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Boo'qwurm]
Priority: Medium

Known:
- id: rarebooqwurm
- category: natural
- breedableOn: Mirror Psychic Island, Psychic Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Boo'qwurm". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Boodoo]
Priority: Medium

Known:
- id: rareboodoo
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Boodoo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Boskus]
Priority: Medium

Known:
- id: rareboskus
- category: amber
- breedableOn: Amber Island, Faerie Island, Fire Oasis, Light Island, Mirror Faerie Island, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Boskus". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Bowgart]
Priority: Medium

Known:
- id: rarebowgart
- category: natural
- breedableOn: Cold Island, Gold Island, Mirror Cold Island, Mirror Plant Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Bowgart". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Bowhead]
Priority: Medium

Known:
- id: rarebowhead
- category: natural
- breedableOn: Amber Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Bowhead". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Bridg-It]
Priority: Medium

Known:
- id: rarebridgit
- category: natural
- breedableOn: Faerie Island, Magical Nexus, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Bridg-It". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Brump]
Priority: Medium

Known:
- id: rarebrump
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Brump". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Bulbo]
Priority: Medium

Known:
- id: rarebulbo
- category: natural
- breedableOn: Light Island, Magical Nexus, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Bulbo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Buzzinga]
Priority: Medium

Known:
- id: rarebuzzinga
- category: natural
- breedableOn: Fire Haven, Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Buzzinga". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Candelavra]
Priority: Medium

Known:
- id: rarecandelavra
- category: natural
- breedableOn: Amber Island, Fire Haven

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Candelavra". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Cantorell]
Priority: Medium

Known:
- id: rarecantorell
- category: natural
- breedableOn: Faerie Island, Magical Nexus, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Cantorell". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Carillong]
Priority: Medium

Known:
- id: rarecarillong
- category: natural
- breedableOn: Magical Sanctum, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Carillong". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Cataliszt]
Priority: Medium

Known:
- id: rarecataliszt
- category: natural
- breedableOn: Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Cataliszt". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Cherubble]
Priority: Medium

Known:
- id: rarecherubble
- category: natural
- breedableOn: Fire Oasis, Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Cherubble". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Clackula]
Priority: Medium

Known:
- id: rareclackula
- category: natural
- breedableOn: Bone Island, Magical Nexus, Magical Sanctum, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Clackula". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Clamble]
Priority: Medium

Known:
- id: rareclamble
- category: natural
- breedableOn: Earth Island, Gold Island, Mirror Earth Island, Mirror Plant Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Clamble". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Clavavera]
Priority: Medium

Known:
- id: rareclavavera
- category: natural
- breedableOn: Bone Island, Mirror Bone Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Clavavera". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Clavi Gnat]
Priority: Medium

Known:
- id: rareclavignat
- category: natural
- breedableOn: Faerie Island, Magical Nexus, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Clavi Gnat". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Congole]
Priority: Medium

Known:
- id: rarecongole
- category: natural
- breedableOn: Air Island, Cold Island, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Congole". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Cranchee]
Priority: Medium

Known:
- id: rarecranchee
- category: natural
- breedableOn: Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Cranchee". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Creepuscule]
Priority: Medium

Known:
- id: rarecreepuscule
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Creepuscule". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Cybop]
Priority: Medium

Known:
- id: rarecybop
- category: natural
- breedableOn: Air Island, Earth Island, Fire Haven, Gold Island, Mirror Air Island, Mirror Earth Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Cybop". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Dandidoo]
Priority: Medium

Known:
- id: raredandidoo
- category: natural
- breedableOn: Cold Island, Earth Island, Fire Haven, Gold Island, Mirror Cold Island, Mirror Earth Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Dandidoo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Deedge]
Priority: Medium

Known:
- id: raredeedge
- category: natural
- breedableOn: Cold Island, Gold Island, Mirror Cold Island, Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Deedge". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Denchuhs]
Priority: Medium

Known:
- id: raredenchuhs
- category: natural
- breedableOn: Bone Island, Magical Nexus, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Denchuhs". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Dermit]
Priority: Medium

Known:
- id: raredermit
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Dermit". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Dragong]
Priority: Medium

Known:
- id: raredragong
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Dragong". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Drummidary]
Priority: Medium

Known:
- id: raredrummidary
- category: natural
- breedableOn: Amber Island, Light Island, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Drummidary". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Drumpler]
Priority: Medium

Known:
- id: raredrumpler
- category: natural
- breedableOn: Air Island, Earth Island, Faerie Island, Gold Island, Mirror Air Island, Mirror Earth Island, Mirror Faerie Island, Mirror Plant Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Drumpler". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Dwumrohl]
Priority: Medium

Known:
- id: raredwumrohl
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Dwumrohl". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Edamimi]
Priority: Medium

Known:
- id: rareedamimi
- category: natural
- breedableOn: Amber Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Edamimi". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Entbrat]
Priority: Medium

Known:
- id: rareentbrat
- category: natural
- breedableOn: Gold Island, Mirror Plant Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Entbrat". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Ffidyll]
Priority: Medium

Known:
- id: rareffidyll
- category: natural
- breedableOn: Faerie Island, Mirror Faerie Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Ffidyll". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Fiddlement]
Priority: Medium

Known:
- id: rarefiddlement
- category: natural
- breedableOn: Light Island, Magical Nexus, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Fiddlement". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Fleechwurm]
Priority: Medium

Known:
- id: rarefleechwurm
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Fleechwurm". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Floogull]
Priority: Medium

Known:
- id: rarefloogull
- category: natural
- breedableOn: Amber Island, Fire Haven

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Floogull". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Floot Fly]
Priority: Medium

Known:
- id: rareflootfly
- category: natural
- breedableOn: Faerie Island, Magical Nexus, Magical Sanctum, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Floot Fly". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Flowah]
Priority: Medium

Known:
- id: rareflowah
- category: natural
- breedableOn: Amber Island, Fire Haven, Light Island, Mirror Light Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Flowah". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Flum Ox]
Priority: Medium

Known:
- id: rareflumox
- category: natural
- breedableOn: Amber Island, Bone Island, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Flum Ox". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Fluoress]
Priority: Medium

Known:
- id: rarefluoress
- category: natural
- breedableOn: Light Island, Magical Nexus, Magical Sanctum, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Fluoress". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Fung Pray]
Priority: Medium

Known:
- id: rarefungpray
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Fung Pray". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Furcorn]
Priority: Medium

Known:
- id: rarefurcorn
- category: natural
- breedableOn: Cold Island, Earth Island, Gold Island, Light Island, Mirror Cold Island, Mirror Earth Island, Mirror Light Island, Mirror Plant Island, Plant Island, Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Furcorn". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Fwog]
Priority: Medium

Known:
- id: rarefwog
- category: natural
- breedableOn: Air Island, Bone Island, Gold Island, Mirror Air Island, Mirror Bone Island, Mirror Plant Island, Mirror Water Island, Plant Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Fwog". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare G'Day]
Priority: Medium

Known:
- id: raregday
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare G'Day". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare G'Joob]
Priority: Medium

Known:
- id: raregjoob
- category: natural
- breedableOn: Mirror Plant Island, Mythical Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare G'Joob". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Ghazt]
Priority: Medium

Known:
- id: rareghazt
- category: natural
- breedableOn: Ethereal Island, Mirror Plant Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Ghazt". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Gheegur]
Priority: Medium

Known:
- id: raregheegur
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Gheegur". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Gloptic]
Priority: Medium

Known:
- id: raregloptic
- category: natural
- breedableOn: Magical Nexus, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Gloptic". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Glowl]
Priority: Medium

Known:
- id: rareglowl
- category: natural
- breedableOn: Amber Island, Fire Haven, Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Glowl". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Gnarls]
Priority: Medium

Known:
- id: raregnarls
- category: natural
- breedableOn: Amber Island, Bone Island, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Gnarls". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Gob]
Priority: Medium

Known:
- id: raregob
- category: natural
- breedableOn: Light Island, Magical Nexus, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Gob". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Gobbleygourd]
Priority: Medium

Known:
- id: raregobbleygourd
- category: natural
- breedableOn: Fire Haven, Fire Oasis, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Gobbleygourd". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Grumpyre]
Priority: Medium

Known:
- id: raregrumpyre
- category: natural
- breedableOn: Cold Island, Ethereal Island, Mirror Cold Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Grumpyre". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Hawlo]
Priority: Medium

Known:
- id: rarehawlo
- category: natural
- breedableOn: Bone Island, Magical Nexus, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Hawlo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare HippityHop]
Priority: Medium

Known:
- id: rarehippityhop
- category: natural
- breedableOn: Faerie Island, Magical Nexus, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare HippityHop". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Hoola]
Priority: Medium

Known:
- id: rarehoola
- category: natural
- breedableOn: Air Island, Earth Island, Gold Island, Mirror Air Island, Mirror Earth Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Hoola". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Humbug]
Priority: Medium

Known:
- id: rarehumbug
- category: natural
- breedableOn: Earth Island, Ethereal Island, Mirror Earth Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Humbug". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Hyehehe]
Priority: Medium

Known:
- id: rarehyehehe
- category: natural
- breedableOn: Earth Island, Mirror Earth Island, Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Hyehehe". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Incisaur]
Priority: Medium

Known:
- id: rareincisaur
- category: natural
- breedableOn: Amber Island, Bone Island, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Incisaur". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Jam Boree]
Priority: Medium

Known:
- id: rarejamboree
- category: natural
- breedableOn: Gold Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Jam Boree". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Jeeode]
Priority: Medium

Known:
- id: rarejeeode
- category: natural
- breedableOn: Ethereal Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Jeeode". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Jellbilly]
Priority: Medium

Known:
- id: rarejellbilly
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Jellbilly". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Kayna]
Priority: Medium

Known:
- id: rarekayna
- category: natural
- breedableOn: Amber Island, Bone Island, Faerie Island, Fire Haven, Fire Oasis, Light Island, Mirror Bone Island, Mirror Faerie Island, Mirror Light Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Kayna". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Kazilleon]
Priority: Medium

Known:
- id: rarekazilleon
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Kazilleon". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Knucklehead]
Priority: Medium

Known:
- id: rareknucklehead
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Knucklehead". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Knurv]
Priority: Medium

Known:
- id: rareknurv
- category: natural
- breedableOn: Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Knurv". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Krillby]
Priority: Medium

Known:
- id: rarekrillby
- category: natural
- breedableOn: Amber Island, Faerie Island, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Krillby". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Mammott]
Priority: Medium

Known:
- id: raremammott
- category: natural
- breedableOn: Air Island, Cold Island, Earth Island, Faerie Island, Fire Oasis, Gold Island, Light Island, Mirror Air Island, Mirror Cold Island, Mirror Earth Island, Mirror Faerie Island, Mirror Light Island, Mirror Plant Island, Plant Island, Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Mammott". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Maulch]
Priority: Medium

Known:
- id: raremaulch
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Maulch". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Maw]
Priority: Medium

Known:
- id: raremaw
- category: natural
- breedableOn: Air Island, Cold Island, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, Mirror Plant Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Maw". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Mimic]
Priority: Medium

Known:
- id: raremimic
- category: natural
- breedableOn: Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Mimic". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Monculus]
Priority: Medium

Known:
- id: raremonculus
- category: natural
- breedableOn: Ethereal Island, Seasonal Shanty, Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Monculus". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Mushaboom]
Priority: Medium

Known:
- id: raremushaboom
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Mushaboom". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Nebulob]
Priority: Medium

Known:
- id: rarenebulob
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Nebulob". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Noggin]
Priority: Medium

Known:
- id: rarenoggin
- category: natural
- breedableOn: Air Island, Bone Island, Earth Island, Faerie Island, Fire Haven, Gold Island, Mirror Air Island, Mirror Bone Island, Mirror Earth Island, Mirror Faerie Island, Mirror Plant Island, Mirror Water Island, Plant Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Noggin". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Oaktopus]
Priority: Medium

Known:
- id: rareoaktopus
- category: natural
- breedableOn: Cold Island, Gold Island, Mirror Cold Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, Shugabush Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Oaktopus". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Osstax]
Priority: Medium

Known:
- id: rareosstax
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Osstax". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Pango]
Priority: Medium

Known:
- id: rarepango
- category: natural
- breedableOn: Air Island, Cold Island, Earth Island, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, Mirror Earth Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Pango". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Phangler]
Priority: Medium

Known:
- id: rarephangler
- category: natural
- breedableOn: Amber Island, Bone Island, Fire Oasis, Mirror Bone Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Phangler". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Pinghound]
Priority: Medium

Known:
- id: rarepinghound
- category: natural
- breedableOn: Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Pinghound". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Pixolotl]
Priority: Medium

Known:
- id: rarepixolotl
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Pixolotl". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Pladdie]
Priority: Medium

Known:
- id: rarepladdie
- category: natural
- breedableOn: Faerie Island, Magical Nexus, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Pladdie". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Plinkajou]
Priority: Medium

Known:
- id: rareplinkajou
- category: natural
- breedableOn: Bone Island, Magical Nexus, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Plinkajou". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Pluckbill]
Priority: Medium

Known:
- id: rarepluckbill
- category: natural
- breedableOn: Light Island, Magical Nexus, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Pluckbill". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Poewk]
Priority: Medium

Known:
- id: rarepoewk
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Poewk". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare PomPom]
Priority: Medium

Known:
- id: rarepompom
- category: natural
- breedableOn: Air Island, Earth Island, Gold Island, Mirror Air Island, Mirror Earth Island, Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare PomPom". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare PongPing]
Priority: Medium

Known:
- id: rarepongping
- category: natural
- breedableOn: Amber Island, Faerie Island, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare PongPing". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Poppette]
Priority: Medium

Known:
- id: rarepoppette
- category: natural
- breedableOn: Magical Nexus, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Poppette". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Potbelly]
Priority: Medium

Known:
- id: rarepotbelly
- category: natural
- breedableOn: Cold Island, Earth Island, Fire Haven, Gold Island, Light Island, Mirror Cold Island, Mirror Earth Island, Mirror Light Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, Shugabush Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Potbelly". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Pummel]
Priority: Medium

Known:
- id: rarepummel
- category: natural
- breedableOn: Gold Island, Mirror Plant Island, Mirror Water Island, Plant Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Pummel". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Punkleton]
Priority: Medium

Known:
- id: rarepunkleton
- category: natural
- breedableOn: Gold Island, Mirror Plant Island, Plant Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Punkleton". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Quarrister]
Priority: Medium

Known:
- id: rarequarrister
- category: natural
- breedableOn: Earth Island, Gold Island, Mirror Earth Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Quarrister". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Quibble]
Priority: Medium

Known:
- id: rarequibble
- category: natural
- breedableOn: Air Island, Cold Island, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, Mirror Water Island, Shugabush Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Quibble". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Reebro]
Priority: Medium

Known:
- id: rarereebro
- category: natural
- breedableOn: Air Island, Ethereal Island, Mirror Air Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Reebro". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Reedling]
Priority: Medium

Known:
- id: rarereedling
- category: natural
- breedableOn: Earth Island, Fire Haven, Gold Island, Mirror Earth Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Reedling". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Repatillo]
Priority: Medium

Known:
- id: rarerepatillo
- category: natural
- breedableOn: Amber Island, Fire Haven

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Repatillo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Riff]
Priority: Medium

Known:
- id: rareriff
- category: natural
- breedableOn: Air Island, Gold Island, Mirror Air Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Riff". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Roarick]
Priority: Medium

Known:
- id: rareroarick
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Roarick". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Rooba]
Priority: Medium

Known:
- id: rarerooba
- category: natural
- breedableOn: Magical Nexus, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Rooba". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Rootitoot]
Priority: Medium

Known:
- id: rarerootitoot
- category: natural
- breedableOn: Amber Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Rootitoot". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Scargo]
Priority: Medium

Known:
- id: rarescargo
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Scargo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Schmoochle]
Priority: Medium

Known:
- id: rareschmoochle
- category: natural
- breedableOn: Air Island, Gold Island, Mirror Air Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Schmoochle". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Screemu]
Priority: Medium

Known:
- id: rarescreemu
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Screemu". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Scups]
Priority: Medium

Known:
- id: rarescups
- category: natural
- breedableOn: Air Island, Gold Island, Mirror Air Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Scups". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Shellbeat]
Priority: Medium

Known:
- id: rareshellbeat
- category: natural
- breedableOn: Gold Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Shellbeat". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare shLep]
Priority: Medium

Known:
- id: rareshlep
- category: natural
- breedableOn: Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare shLep". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Shrubb]
Priority: Medium

Known:
- id: rareshrubb
- category: natural
- breedableOn: Earth Island, Fire Haven, Gold Island, Mirror Earth Island, Mirror Plant Island, Mirror Water Island, Plant Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Shrubb". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Sneyser]
Priority: Medium

Known:
- id: raresneyser
- category: natural
- breedableOn: Amber Island, Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Sneyser". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Sooza]
Priority: Medium

Known:
- id: raresooza
- category: natural
- breedableOn: Amber Island, Light Island, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Sooza". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Sox]
Priority: Medium

Known:
- id: raresox
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Sox". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Sporerow]
Priority: Medium

Known:
- id: raresporerow
- category: natural
- breedableOn: Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Sporerow". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Spunge]
Priority: Medium

Known:
- id: rarespunge
- category: natural
- breedableOn: Cold Island, Gold Island, Mirror Cold Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Spunge". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Spurrit]
Priority: Medium

Known:
- id: rarespurrit
- category: natural
- breedableOn: Fire Oasis, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Spurrit". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Spytrap]
Priority: Medium

Known:
- id: rarespytrap
- category: natural
- breedableOn: Light Island, Magical Nexus, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Spytrap". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Stogg]
Priority: Medium

Known:
- id: rarestogg
- category: natural
- breedableOn: Amber Island, Bone Island, Faerie Island, Fire Haven, Mirror Bone Island, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Stogg". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Strombonin]
Priority: Medium

Known:
- id: rarestrombonin
- category: natural
- breedableOn: Cold Island, Mirror Cold Island, Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Strombonin". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare T-Rox]
Priority: Medium

Known:
- id: raretrox
- category: natural
- breedableOn: Air Island, Gold Island, Mirror Air Island, Mirror Plant Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare T-Rox". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Tapricorn]
Priority: Medium

Known:
- id: raretapricorn
- category: natural
- breedableOn: Magical Nexus, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Tapricorn". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Theremind]
Priority: Medium

Known:
- id: raretheremind
- category: natural
- breedableOn: Magical Nexus, Magical Sanctum, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Theremind". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Thrumble]
Priority: Medium

Known:
- id: rarethrumble
- category: natural
- breedableOn: Amber Island, Bone Island, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Thrumble". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Thumpies]
Priority: Medium

Known:
- id: rarethumpies
- category: natural
- breedableOn: Cold Island, Earth Island, Gold Island, Mirror Cold Island, Mirror Earth Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Thumpies". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Thwok]
Priority: Medium

Known:
- id: rarethwok
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Thwok". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Tiawa]
Priority: Medium

Known:
- id: raretiawa
- category: natural
- breedableOn: Amber Island, Light Island, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Tiawa". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Toe Jammer]
Priority: Medium

Known:
- id: raretoejammer
- category: natural
- breedableOn: Air Island, Bone Island, Cold Island, Fire Oasis, Gold Island, Mirror Air Island, Mirror Bone Island, Mirror Cold Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Toe Jammer". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare TooToo]
Priority: Medium

Known:
- id: raretootoo
- category: natural
- breedableOn: Light Island, Magical Nexus, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare TooToo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Tring]
Priority: Medium

Known:
- id: raretring
- category: natural
- breedableOn: Amber Island, Fire Haven

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Tring". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Tuskski]
Priority: Medium

Known:
- id: raretuskski
- category: natural
- breedableOn: Amber Island, Faerie Island, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Tuskski". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Tweedle]
Priority: Medium

Known:
- id: raretweedle
- category: natural
- breedableOn: Air Island, Cold Island, Earth Island, Fire Haven, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, Mirror Earth Island, Mirror Water Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Tweedle". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Tympa]
Priority: Medium

Known:
- id: raretympa
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Tympa". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Uuduk]
Priority: Medium

Known:
- id: rareuuduk
- category: natural
- breedableOn: Bone Island, Magical Nexus, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Uuduk". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Viveine]
Priority: Medium

Known:
- id: rareviveine
- category: natural
- breedableOn: Amber Island, Seasonal Shanty, Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Viveine". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Whaddle]
Priority: Medium

Known:
- id: rarewhaddle
- category: natural
- breedableOn: Amber Island, Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Whaddle". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Whajje]
Priority: Medium

Known:
- id: rarewhajje
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Whajje". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Whisp]
Priority: Medium

Known:
- id: rarewhisp
- category: natural
- breedableOn: Ethereal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Whisp". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Whiz-bang]
Priority: Medium

Known:
- id: rarewhizbang
- category: natural
- breedableOn: Light Island, Mirror Light Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Whiz-bang". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Wimmzies]
Priority: Medium

Known:
- id: rarewimmzies
- category: natural
- breedableOn: Faerie Island, Magical Nexus, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Wimmzies". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Withur]
Priority: Medium

Known:
- id: rarewithur
- category: natural
- breedableOn: Bone Island, Magical Nexus, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Withur". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Woolabee]
Priority: Medium

Known:
- id: rarewoolabee
- category: natural
- breedableOn: Amber Island, Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Woolabee". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Wubbox]
Priority: Medium

Known:
- id: rarewubbox
- category: natural
- breedableOn: Air Island, Cold Island, Earth Island, Ethereal Island, Fire Haven, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, Mirror Earth Island, Mirror Plant Island, Mirror Water Island, Plant Island, Water Island, Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Wubbox". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Wynq]
Priority: Medium

Known:
- id: rarewynq
- category: natural
- breedableOn: Amber Island, Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Wynq". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Xyster]
Priority: Medium

Known:
- id: rarexyster
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Xyster". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Yawstrich]
Priority: Medium

Known:
- id: rareyawstrich
- category: natural
- breedableOn: Air Island, Mirror Air Island, Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Yawstrich". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Yelmut]
Priority: Medium

Known:
- id: rareyelmut
- category: natural
- breedableOn: Amber Island, Light Island, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Yelmut". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Yool]
Priority: Medium

Known:
- id: rareyool
- category: natural
- breedableOn: Cold Island, Gold Island, Mirror Cold Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Yool". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Yuggler]
Priority: Medium

Known:
- id: rareyuggler
- category: natural
- breedableOn: Magical Nexus, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Yuggler". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Ziggurab]
Priority: Medium

Known:
- id: rareziggurab
- category: amber
- breedableOn: Amber Island, Faerie Island, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Ziggurab". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Zuuker]
Priority: Medium

Known:
- id: rarezuuker
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Zuuker". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rare Zynth]
Priority: Medium

Known:
- id: rarezynth
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rare Zynth". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Re]
Priority: Medium

Known:
- id: re
- category: natural
- breedableOn: Air Island, Bone Island, Cold Island, Earth Island, Ethereal Island, Faerie Island, Fire Haven, Fire Oasis, Light Island, Magical Sanctum, Mirror Air Island, Mirror Bone Island, Mirror Cold Island, Mirror Earth Island, Mirror Faerie Island, Mirror Light Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Re". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Reebro]
Priority: Medium

Known:
- id: reebro
- category: natural
- breedableOn: Air Island, Ethereal Island, Mirror Air Island, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Reebro". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Reedling]
Priority: Medium

Known:
- id: reedling
- category: natural
- breedableOn: Earth Island, Fire Haven, Gold Island, Mirror Earth Island, Mirror Water Island, The Colossingum, Tribal Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Reedling". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Repatillo]
Priority: Medium

Known:
- id: repatillo
- category: natural
- breedableOn: Amber Island, Fire Haven

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Repatillo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rhysmuth]
Priority: Medium

Known:
- id: rhysmuth
- category: natural
- breedableOn: Ethereal Workshop

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rhysmuth". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Roarick]
Priority: Medium

Known:
- id: roarick
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Roarick". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rooba]
Priority: Medium

Known:
- id: rooba
- category: natural
- breedableOn: Magical Nexus, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rooba". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Rootitoot]
Priority: Medium

Known:
- id: rootitoot
- category: natural
- breedableOn: Amber Island, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Rootitoot". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Scaratar]
Priority: Medium

Known:
- id: scaratar
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Scaratar". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Scargo]
Priority: Medium

Known:
- id: scargo
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Scargo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Schmoochle]
Priority: Medium

Known:
- id: schmoochle
- category: natural
- breedableOn: Air Island, Gold Island, Mirror Air Island, Seasonal Shanty, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Schmoochle". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Screemu]
Priority: Medium

Known:
- id: screemu
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Screemu". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Scups]
Priority: Medium

Known:
- id: scups
- category: natural
- breedableOn: Air Island, Gold Island, Mirror Air Island, Mirror Water Island, The Colossingum, Tribal Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Scups". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Shellbeat]
Priority: Medium

Known:
- id: shellbeat
- category: natural
- breedableOn: Gold Island, Mirror Water Island, The Colossingum, Tribal Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Shellbeat". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[shLep]
Priority: Medium

Known:
- id: shlep
- category: natural
- breedableOn: Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "shLep". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Shugabass]
Priority: Medium

Known:
- id: shugabass
- category: natural
- breedableOn: Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Shugabass". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Shugabeats]
Priority: Medium

Known:
- id: shugabeats
- category: natural
- breedableOn: Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Shugabeats". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Shugabush]
Priority: Medium

Known:
- id: shugabush
- category: natural
- breedableOn: Gold Island, Mirror Plant Island, Plant Island, Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Shugabush". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Shugabuzz]
Priority: Medium

Known:
- id: shugabuzz
- category: natural
- breedableOn: Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Shugabuzz". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Shugajo]
Priority: Medium

Known:
- id: shugajo
- category: natural
- breedableOn: Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Shugajo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Shugarock]
Priority: Medium

Known:
- id: shugarock
- category: natural
- breedableOn: Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Shugarock". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Shugavox]
Priority: Medium

Known:
- id: shugavox
- category: natural
- breedableOn: Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Shugavox". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Shugitar]
Priority: Medium

Known:
- id: shugitar
- category: natural
- breedableOn: Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Shugitar". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Sneyser]
Priority: Medium

Known:
- id: sneyser
- category: natural
- breedableOn: Amber Island, Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Sneyser". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Sol]
Priority: Medium

Known:
- id: sol
- category: natural
- breedableOn: Air Island, Bone Island, Cold Island, Earth Island, Ethereal Island, Faerie Island, Fire Haven, Fire Oasis, Light Island, Magical Sanctum, Mirror Air Island, Mirror Bone Island, Mirror Cold Island, Mirror Earth Island, Mirror Faerie Island, Mirror Light Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Sol". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Sooza]
Priority: Medium

Known:
- id: sooza
- category: natural
- breedableOn: Amber Island, Light Island, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Sooza". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Sox]
Priority: Medium

Known:
- id: sox
- category: natural
- breedableOn: Ethereal Island, Plasma Islet, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Sox". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Sporerow]
Priority: Medium

Known:
- id: sporerow
- category: natural
- breedableOn: Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Sporerow". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Spunge]
Priority: Medium

Known:
- id: spunge
- category: natural
- breedableOn: Cold Island, Gold Island, Mirror Cold Island, Mirror Water Island, The Colossingum, Tribal Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Spunge". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Spurrit]
Priority: Medium

Known:
- id: spurrit
- category: natural
- breedableOn: Fire Oasis, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Spurrit". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Spytrap]
Priority: Medium

Known:
- id: spytrap
- category: natural
- breedableOn: Light Island, Magical Nexus, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Spytrap". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Squot]
Priority: Medium

Known:
- id: squot
- category: natural
- breedableOn: Faerie Island, Magical Nexus, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Squot". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Stoowarb]
Priority: Medium

Known:
- id: stoowarb
- category: natural
- breedableOn: Air Island, Earth Island, Mirror Air Island, Mirror Earth Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Stoowarb". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Strombonin]
Priority: Medium

Known:
- id: strombonin
- category: natural
- breedableOn: Cold Island, Mirror Cold Island, Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Strombonin". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Syncopite]
Priority: Medium

Known:
- id: syncopite
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Syncopite". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Tapricorn]
Priority: Medium

Known:
- id: tapricorn
- category: natural
- breedableOn: Magical Nexus, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Tapricorn". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Tawkerr]
Priority: Medium

Known:
- id: tawkerr
- category: natural
- breedableOn: Cold Island, Earth Island, Mirror Cold Island, Mirror Earth Island, Mirror Plant Island, Plant Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Tawkerr". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Teeter-Tauter]
Priority: Medium

Known:
- id: teetertauter
- category: natural
- breedableOn: Ethereal Workshop

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Teeter-Tauter". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Theremind]
Priority: Medium

Known:
- id: theremind
- category: natural
- breedableOn: Magical Nexus, Magical Sanctum, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Theremind". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Thumpies]
Priority: Medium

Known:
- id: thumpies
- category: natural
- breedableOn: Cold Island, Earth Island, Gold Island, Mirror Cold Island, Mirror Earth Island, The Colossingum, Tribal Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Thumpies". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Thwok]
Priority: Medium

Known:
- id: thwok
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Thwok". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Ti]
Priority: Medium

Known:
- id: ti
- category: natural
- breedableOn: Air Island, Bone Island, Cold Island, Earth Island, Ethereal Island, Faerie Island, Fire Haven, Fire Oasis, Light Island, Magical Sanctum, Mirror Air Island, Mirror Bone Island, Mirror Cold Island, Mirror Earth Island, Mirror Faerie Island, Mirror Light Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Ti". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Tiawa]
Priority: Medium

Known:
- id: tiawa
- category: natural
- breedableOn: Amber Island, Light Island, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Tiawa". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Toe Jammer]
Priority: Medium

Known:
- id: toejammer
- category: natural
- breedableOn: Air Island, Bone Island, Cold Island, Fire Oasis, Gold Island, Mirror Air Island, Mirror Bone Island, Mirror Cold Island, Mirror Plant Island, Mirror Psychic Island, Mirror Water Island, Plant Island, Psychic Island, The Colossingum, Tribal Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Toe Jammer". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[TooToo]
Priority: Medium

Known:
- id: tootoo
- category: natural
- breedableOn: Light Island, Magical Nexus, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "TooToo". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Torrt]
Priority: Medium

Known:
- id: torrt
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Torrt". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Tring]
Priority: Medium

Known:
- id: tring
- category: natural
- breedableOn: Amber Island, Fire Haven

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Tring". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Tuskski]
Priority: Medium

Known:
- id: tuskski
- category: natural
- breedableOn: Amber Island, Faerie Island, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Tuskski". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Tweedle]
Priority: Medium

Known:
- id: tweedle
- category: natural
- breedableOn: Air Island, Cold Island, Earth Island, Fire Haven, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, Mirror Earth Island, Mirror Water Island, The Colossingum, Tribal Island, Water Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Tweedle". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Tympa]
Priority: Medium

Known:
- id: tympa
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Tympa". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Uuduk]
Priority: Medium

Known:
- id: uuduk
- category: natural
- breedableOn: Bone Island, Magical Nexus, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Uuduk". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Vhamp]
Priority: Medium

Known:
- id: vhamp
- category: natural
- breedableOn: Celestial Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Vhamp". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Vhenshun]
Priority: Medium

Known:
- id: vhenshun
- category: natural
- breedableOn: Ethereal Workshop

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Vhenshun". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Viveine]
Priority: Medium

Known:
- id: viveine
- category: natural
- breedableOn: Amber Island, Seasonal Shanty, Shugabush Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Viveine". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Whaddle]
Priority: Medium

Known:
- id: whaddle
- category: natural
- breedableOn: Amber Island, Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Whaddle". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Whaill]
Priority: Medium

Known:
- id: whaill
- category: natural
- breedableOn: Ethereal Workshop, Plasma Islet

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Whaill". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Whajje]
Priority: Medium

Known:
- id: whajje
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Whajje". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Wheezel]
Priority: Medium

Known:
- id: wheezel
- category: natural
- breedableOn: Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Wheezel". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Whisp]
Priority: Medium

Known:
- id: whisp
- category: natural
- breedableOn: Ethereal Island, Plasma Islet, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Whisp". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Whiz-bang]
Priority: Medium

Known:
- id: whizbang
- category: natural
- breedableOn: Light Island, Mirror Light Island, Seasonal Shanty

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Whiz-bang". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Wimmzies]
Priority: Medium

Known:
- id: wimmzies
- category: natural
- breedableOn: Faerie Island, Magical Nexus, Mirror Faerie Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Wimmzies". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Withur]
Priority: Medium

Known:
- id: withur
- category: natural
- breedableOn: Bone Island, Magical Nexus, Mirror Bone Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Withur". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Woolabee]
Priority: Medium

Known:
- id: woolabee
- category: natural
- breedableOn: Amber Island, Fire Oasis

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Woolabee". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Wubbox]
Priority: Medium

Known:
- id: wubbox
- category: natural
- breedableOn: Air Island, Cold Island, Earth Island, Ethereal Island, Fire Haven, Fire Oasis, Gold Island, Mirror Air Island, Mirror Cold Island, Mirror Earth Island, Mirror Plant Island, Mirror Water Island, Plant Island, Water Island, Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Wubbox". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[X'rt]
Priority: Medium

Known:
- id: xrt
- category: natural
- breedableOn: Ethereal Workshop

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "X'rt". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Xyster]
Priority: Medium

Known:
- id: xyster
- category: natural
- breedableOn: Magical Sanctum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Xyster". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Yawstrich]
Priority: Medium

Known:
- id: yawstrich
- category: natural
- breedableOn: Air Island, Mirror Air Island, Mythical Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Yawstrich". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Yelmut]
Priority: Medium

Known:
- id: yelmut
- category: natural
- breedableOn: Amber Island, Light Island, Mirror Light Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Yelmut". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Yool]
Priority: Medium

Known:
- id: yool
- category: natural
- breedableOn: Cold Island, Gold Island, Mirror Cold Island, Seasonal Shanty, The Colossingum

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Yool". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Yooreek]
Priority: Medium

Known:
- id: yooreek
- category: natural
- breedableOn: Ethereal Workshop, Plasma Islet

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Yooreek". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Yuggler]
Priority: Medium

Known:
- id: yuggler
- category: natural
- breedableOn: Magical Nexus, Mirror Psychic Island, Psychic Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Yuggler". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Zuuker]
Priority: Medium

Known:
- id: zuuker
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Zuuker". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

[Zynth]
Priority: Medium

Known:
- id: zynth
- category: natural
- breedableOn: Wublin Island

Missing:
- elements
- description
- class
- combo
- aliases
- releaseIsland
- notes
- isLimited
- availabilityNotes

Why this matters:
- Improves filtering, grouping, and future search behavior across monster-facing UI.
- Adds clearer context for reference screens without requiring extra explanatory UI text.

Prompt:
Provide high-confidence My Singing Monsters metadata for "Zynth". Return only a compact JSON object with these fields: { "elements": ..., "description": ..., "class": ..., "combo": ..., "aliases": ..., "releaseIsland": ..., "notes": ..., "isLimited": ..., "availabilityNotes": ... } Use concise values, no long explanations, and leave uncertain fields empty or null.

---

LOW PRIORITY

No monsters currently queued.
